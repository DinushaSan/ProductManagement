pipeline {
    agent {
        docker {
            image 'maven:3.8.5' // Correct Maven version
            args '--privileged'
        }
    }
    stages {
        stage('Maven Install') {
            steps {
                sh 'mvn clean install'
            }
        }
    }
}

