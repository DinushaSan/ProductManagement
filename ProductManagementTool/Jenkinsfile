#!groovy
pipeline {
    agent {
        docker {
            image 'maven:4.0.0'
            args '--privileged'
        }
    }
    stages {
        stage('Maven Install') {
            steps {
                sh 'mvn clean install'
            }
        }
    }
}